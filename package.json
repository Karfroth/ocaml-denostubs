{
  "name": "denostubs",
  "version": "0.0.1",
  "description": "Deno-ffi stubs generator",
  "license": "MIT",
  "esy": {
    "build": "dune build -p #{self.name}",
    "release": {
      "bin": "denostubs"
    }
  },
  "scripts": {
    "test": ""
  },
  "dependencies": {
    "@opam/ctypes": "0.15.0",
    "ocaml": "^4.13.1"
  },
  "devDependencies": {
    "@opam/ocaml-lsp-server": "*",
    "@opam/ocamlfind-secondary": "*",
    "@opam/dune": "*",
    "@opam/lambda-term": "*",
    "ocaml": "^4.13.1"
  }
}